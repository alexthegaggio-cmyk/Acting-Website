# StageReady Acting Course

This is my project for the StageReady acting course website.

## How to run the project

I have included a `run.sh` script to make the setup easier. To start the website, open your terminal in this folder and type:

```bash
bash run.sh
```

The script will automatically set up the environment and start the server at **http://127.0.0.1:5000**.